1.semVer: semantic versioning. 
It includes: major(big change), minor(new features added), patch version(bugs fixed).

major components invloved in node.js env:
v8 engine: 
uses JIT(just in time) compilation. 
converts the js to machine language.
It only handles call stack and memory heap.

libUv:
it's a completely C library.
it handles Event Loop, Async I/O, Timers, os, async I/O.
since, js is single threaded. libUv provides the external threads.
node bindings acts as a bridge between js and libUv.

compiler v/s interpreter:
| Feature         | Compiler                                | Interpreter                   |
| --------------- | --------------------------------------- | ----------------------------- |
| Translation     | Converts **entire code at once**        | Converts **line by line**     |
| Execution       | Runs after compilation                  | Executes immediately          |
| Speed           | Faster execution                        | Slower execution              |
| Error detection | Shows errors **after full compilation** | Shows errors **line by line** |

> globalThis refers to the global object of the current runtime environment.
> typeof null is an object.
> parseInt converts the strings into number and ignores zeros.
> usecase of spread operator in objects and arrays are slightly different.
> node monitores the file : --watch filename.js 

NPM: is a package mangager where libraries, framewroks are available.
. installed dependencies(libraries) & versions are stored inside package.json.
. code of the installed dependencies are stored inside node modules.
. exact version of dependencies are stored inside package.lock.json.
. if any package shows "DT" in npm then it doesn't have types. (e.g: express). ***
. use "npm i @types/packagename -D" for installing types to the package. ***
. use "npm i @types/node -D" for node built in packgaes. ***